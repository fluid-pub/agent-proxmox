name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - develop

permissions:
  contents: read

jobs:
  ci:
    uses: fluid-pub/actions/.github/workflows/go-workload-ci.yml@v1
    with:
      workload_kind: agent
      core_repository: fluid-pub/agent-core
      core_ref: develop
